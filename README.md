# generate-feature-flag-summary

Generates Confluence page with feature flag summary from LaunchDarkly.

##  Usage

To run:

* Copy `.env.example` to `.env` and fill in the values
* `npm install`
* `npx tsx src/index.tsx`
